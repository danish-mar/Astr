<script src="/js/pages/accounting.js"></script>

<div x-data="accountingData()" x-init="init()" class="space-y-8 animate__animated animate__fadeIn">
    <!-- Header Section -->
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-6 pb-2">
        <div class="flex items-center gap-4">
            <div class="w-10 h-10 rounded-xl bg-gray-900 flex items-center justify-center text-white shadow-lg">
                <i class="fa-solid fa-chart-pie-simple text-sm"></i>
            </div>
            <div>
                <h1 class="text-2xl font-bold text-gray-900 tracking-tight">Financial Gateway</h1>
                <p class="text-[10px] font-bold text-gray-400 uppercase tracking-[0.2em] mt-0.5">Professional Accounting
                    Suite</p>
            </div>
        </div>
        <div class="flex items-center gap-2">
            <button @click="openAddAccountModal()"
                class="px-5 py-2.5 rounded-xl bg-white border border-gray-100 text-gray-600 font-bold text-[10px] uppercase tracking-widest hover:shadow-ios transition-all flex items-center gap-2">
                <i class="fas fa-address-book"></i>
                <span>Open Master Ledger</span>
            </button>
            <button @click="openAddTransactionModal()"
                class="px-5 py-2.5 rounded-xl bg-gray-900 text-white font-bold text-[10px] uppercase tracking-widest hover:shadow-2xl transition-all flex items-center gap-2 active:scale-95">
                <i class="fas fa-plus"></i>
                <span>Voucher Entry</span>
            </button>
        </div>
    </div>

    <!-- Tally-Style Horizontal Metric Bar -->
    <div
        class="bg-white border border-gray-100 rounded-[32px] p-2 shadow-sm flex flex-col md:flex-row divide-y md:divide-y-0 md:divide-x divide-gray-50">
        <!-- Outflow Segment -->
        <div class="flex-1 p-6 space-y-1 group hover:bg-gray-50/50 transition-all rounded-l-[28px]">
            <div class="flex items-center gap-2">
                <div class="w-1.5 h-1.5 rounded-full bg-rose-500"></div>
                <span class="text-[9px] font-bold text-gray-400 uppercase tracking-widest leading-none">Total Outflow
                    (Payable)</span>
            </div>
            <div class="flex items-baseline gap-1">
                <span class="text-lg font-bold text-gray-400 font-sans">₹</span>
                <span class="text-3xl font-black tracking-tighter text-gray-900"
                    x-text="formatNumber(summary.totalPayable)">0</span>
            </div>
        </div>
        <!-- Inflow Segment -->
        <div class="flex-1 p-6 space-y-1 group hover:bg-gray-50/50 transition-all">
            <div class="flex items-center gap-2">
                <div class="w-1.5 h-1.5 rounded-full bg-emerald-500"></div>
                <span class="text-[9px] font-bold text-gray-400 uppercase tracking-widest leading-none">Total Inflow
                    (Receivable)</span>
            </div>
            <div class="flex items-baseline gap-1">
                <span class="text-lg font-bold text-gray-400 font-sans">₹</span>
                <span class="text-3xl font-black tracking-tighter text-gray-900"
                    x-text="formatNumber(summary.totalReceivable)">0</span>
            </div>
        </div>
        <!-- Net Status Segment -->
        <div class="flex-1 p-6 space-y-1 group transition-all rounded-r-[28px]"
            :class="summary.netBalance > 0 ? 'bg-rose-50' : 'bg-emerald-50'">
            <div class="flex items-center gap-2">
                <div class="w-1.5 h-1.5 rounded-full"
                    :class="summary.netBalance > 0 ? 'bg-rose-600' : 'bg-emerald-600'"></div>
                <span class="text-[9px] font-bold uppercase tracking-widest leading-none"
                    :class="summary.netBalance > 0 ? 'text-rose-600' : 'text-emerald-600'">Net Position</span>
            </div>
            <div class="flex items-baseline gap-1">
                <span class="text-lg font-black font-sans"
                    :class="summary.netBalance > 0 ? 'text-rose-900' : 'text-emerald-900'">₹</span>
                <span class="text-3xl font-black tracking-tighter"
                    :class="summary.netBalance > 0 ? 'text-rose-900' : 'text-emerald-900'"
                    x-text="formatNumber(summary.netBalance)">0</span>
            </div>
        </div>
    </div>

    <!-- Main Content: Ledger Navigator -->
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 h-[600px]">
        <!-- Accounts Sidebar -->
        <div
            class="lg:col-span-4 bg-white rounded-[32px] border border-gray-100 shadow-sm flex flex-col overflow-hidden">
            <div
                class="p-6 border-b border-gray-50 uppercase tracking-widest text-[10px] font-bold text-gray-400 flex items-center justify-between bg-gray-50/10">
                <div class="flex items-center gap-2">
                    <span>Master Ledger</span>
                    <button @click="modals.manageTags = true"
                        class="text-indigo-500 hover:text-indigo-700 transition-colors" title="Manage Tags">
                        <i class="fas fa-tags"></i>
                    </button>
                </div>
                <div class="relative">
                    <input type="text" x-model="searchQuery" placeholder="Search..."
                        class="bg-white border border-gray-100 focus:border-gray-300 text-[10px] px-3 py-1.5 rounded-xl outline-none transition-all w-24 focus:w-32 shadow-sm">
                </div>
            </div>
            <div class="flex-1 overflow-y-auto custom-scrollbar p-2 space-y-1">
                <template x-for="acc in filteredAccounts" :key="acc._id">
                    <button @click="selectAccount(acc)"
                        :class="selectedAccount?._id === acc._id ? 'bg-gray-900 text-white shadow-xl translate-x-1' : 'text-gray-600 hover:bg-gray-50'"
                        class="w-full flex items-center justify-between p-4 rounded-2xl transition-all group">
                        <div class="flex items-center gap-4 text-left">
                            <div class="w-10 h-10 rounded-xl flex items-center justify-center font-bold text-sm shadow-sm"
                                :class="selectedAccount?._id === acc._id ? 'bg-white/10' : 'bg-gray-50'"
                                x-text="acc.contact?.name?.charAt(0) || 'A'"></div>
                            <div>
                                <p class="text-sm font-bold truncate max-w-[120px]" x-text="acc.accountName"></p>
                                <p class="text-[10px] uppercase tracking-widest opacity-60"
                                    x-text="acc.contact?.companyName || 'Person'"></p>
                            </div>
                        </div>
                        <div class="text-right">
                            <p class="text-xs font-bold" x-text="'₹' + formatNumber(acc.totalBalance)"></p>
                            <p class="text-[8px] uppercase tracking-[0.2em] opacity-40">Balance</p>
                        </div>
                    </button>
                </template>
            </div>
        </div>

        <!-- Ledger View -->
        <div
            class="lg:col-span-8 bg-white rounded-[32px] border border-gray-100 shadow-sm flex flex-col overflow-hidden">
            <template x-if="selectedAccount">
                <div class="flex flex-col h-full">
                    <!-- Ledger Header -->
                    <div
                        class="p-8 border-b border-gray-50 flex flex-col md:flex-row gap-6 justify-between bg-gray-50/10">
                        <div>
                            <div class="flex items-center gap-2 mb-1">
                                <h3 class="text-xl font-bold text-gray-900" x-text="selectedAccount.accountName"></h3>
                                <span
                                    class="px-2 py-0.5 bg-gray-50 border border-gray-100 text-gray-400 text-[9px] font-bold rounded uppercase tracking-widest"
                                    x-text="selectedAccount.contact?.contactType"></span>
                            </div>
                            <p class="text-xs font-medium text-gray-400"
                                x-text="'Tracking all give & take with ' + selectedAccount.contact?.name"></p>
                        </div>

                        <!-- Actions & Date Filtering -->
                        <div class="flex items-center gap-3">
                            <button @click="editAccount(selectedAccount)"
                                class="w-10 h-10 rounded-xl bg-gray-50 text-gray-400 hover:bg-gray-900 hover:text-white transition-all flex items-center justify-center shadow-sm"
                                title="Edit Ledger Settings">
                                <i class="fas fa-cog text-xs"></i>
                            </button>
                            <button @click="confirmDeleteAccount()"
                                class="w-10 h-10 rounded-xl bg-rose-50 text-rose-400 hover:bg-rose-500 hover:text-white transition-all flex items-center justify-center shadow-sm">
                                <i class="fas fa-trash-alt text-xs"></i>
                            </button>
                            <div
                                class="flex items-center gap-2 bg-white border border-gray-100 p-2 rounded-2xl shadow-sm">
                                <div class="flex flex-col gap-1">
                                    <span class="text-[8px] font-bold text-gray-400 uppercase px-1">From</span>
                                    <input type="date" x-model="filter.startDate"
                                        @change="loadLedger(selectedAccount._id)"
                                        class="text-[10px] font-bold bg-transparent outline-none">
                                </div>
                                <div class="w-px h-6 bg-gray-100 mx-1"></div>
                                <div class="flex flex-col gap-1">
                                    <span class="text-[8px] font-bold text-gray-400 uppercase px-1">To</span>
                                    <input type="date" x-model="filter.endDate"
                                        @change="loadLedger(selectedAccount._id)"
                                        class="text-[10px] font-bold bg-transparent outline-none">
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Period Summary -->
                    <div x-show="periodStats"
                        class="px-8 py-4 bg-gray-50 border-b border-gray-100 flex items-center justify-between animate__animated animate__fadeIn">
                        <div class="flex items-center gap-8">
                            <div>
                                <p class="text-[9px] font-bold text-gray-400 uppercase tracking-widest mb-1">Period Gave
                                    (-)</p>
                                <p class="font-bold text-emerald-600">₹<span
                                        x-text="formatNumber(periodStats.totalGave)">0</span></p>
                            </div>
                            <div class="w-px h-8 bg-gray-200"></div>
                            <div>
                                <p class="text-[9px] font-bold text-gray-400 uppercase tracking-widest mb-1">Period Took
                                    (+)</p>
                                <p class="font-bold text-rose-600">₹<span
                                        x-text="formatNumber(periodStats.totalTook)">0</span></p>
                            </div>
                        </div>
                        <div class="text-right">
                            <p class="text-[9px] font-bold text-gray-400 uppercase tracking-widest mb-1">Entries</p>
                            <p class="font-bold text-gray-900" x-text="periodStats.count">0</p>
                        </div>
                    </div>

                    <!-- Ledger Entries -->
                    <div class="flex-1 overflow-y-auto custom-scrollbar">
                        <table class="w-full text-left">
                            <thead class="sticky top-0 bg-white shadow-sm z-10">
                                <tr class="text-[10px] font-bold text-gray-300 uppercase tracking-widest">
                                    <th class="px-8 py-4">Date & Slip</th>
                                    <th class="px-8 py-4">What happened?</th>
                                    <th class="px-8 py-4 text-right">You Gave (-)</th>
                                    <th class="px-8 py-4 text-right">You Took (+)</th>
                                    <th class="px-8 py-4 text-center">Actions</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-50">
                                <template x-for="tx in ledgerTransactions" :key="tx._id">
                                    <tr class="hover:bg-gray-50/50 transition-colors group">
                                        <td class="px-8 py-5">
                                            <p class="text-xs font-bold text-gray-900" x-text="formatDate(tx.date)"></p>
                                            <p class="text-[10px] font-medium text-gray-400"
                                                x-text="tx.reference || 'Personal'"></p>
                                        </td>
                                        <td class="px-8 py-5">
                                            <div class="flex flex-col">
                                                <span class="text-xs font-medium text-gray-600"
                                                    x-text="tx.description || '--'"></span>
                                                <template x-if="tx.tag">
                                                    <span :style="`color: ${tx.tag.color}`"
                                                        class="text-[8px] font-bold uppercase tracking-widest mt-1"
                                                        x-text="'#' + tx.tag.name"></span>
                                                </template>
                                            </div>
                                        </td>
                                        <!-- Gave (Debit) -->
                                        <td class="px-8 py-5 text-right font-bold text-emerald-600 text-[13px]"
                                            x-text="tx.transactionType === 'Debit' ? '₹' + formatNumber(tx.amount) : ''">
                                        </td>
                                        <!-- Took (Credit) -->
                                        <td class="px-8 py-5 text-right font-bold text-rose-600 text-[13px]"
                                            x-text="tx.transactionType === 'Credit' ? '₹' + formatNumber(tx.amount) : ''">
                                        </td>
                                        <td class="px-8 py-5 text-center">
                                            <div
                                                class="flex items-center justify-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
                                                <button @click="editTransaction(tx)"
                                                    class="p-2 text-blue-400 hover:text-blue-600 transition-colors">
                                                    <i class="fas fa-edit text-[10px]"></i>
                                                </button>
                                                <button @click="deleteTransaction(tx._id)"
                                                    class="p-2 text-rose-400 hover:text-rose-600 transition-colors">
                                                    <i class="fas fa-trash-alt text-[10px]"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                </template>
                            </tbody>
                        </table>
                        <template x-if="ledgerTransactions.length === 0">
                            <div class="p-16 text-center text-gray-300 space-y-4">
                                <i class="fas fa-ghost text-4xl opacity-20"></i>
                                <p class="text-sm font-medium italic">No transactions in this period.</p>
                            </div>
                        </template>
                    </div>
                </div>
            </template>
            <template x-if="!selectedAccount">
                <div class="flex flex-col items-center justify-center h-full text-center p-12 space-y-6">
                    <div class="w-24 h-24 bg-gray-50 rounded-[40px] flex items-center justify-center text-gray-200">
                        <i class="fa-solid fa-indian-rupee-sign text-4xl"></i>
                    </div>
                    <div>
                        <p class="text-lg font-bold text-gray-900">Open a Khata</p>
                        <p class="text-gray-400 text-sm max-w-[240px]">Select a person or shop from the left to manage
                            give & take history.</p>
                    </div>
                </div>
            </template>
        </div>
    </div>

    <!-- Modals -->
    <!-- Open Account Modal -->
    <div x-show="modals.addAccount" class="fixed inset-0 z-50 flex items-center justify-center p-6" x-cloak>
        <div @click="modals.addAccount = false"
            class="absolute inset-0 bg-black/20 backdrop-blur-sm animate__animated animate__fadeIn"></div>
        <div class="relative w-full max-w-sm bg-white rounded-[40px] shadow-2xl p-10 animate__animated animate__zoomIn">
            <h2 class="text-2xl font-bold text-gray-900 mb-8">Setup Ledger</h2>
            <form @submit.prevent="createAccount()" class="space-y-6">
                <!-- Simple contact selection in a real app would be a searchable dropdown -->
                <div class="space-y-2">
                    <label class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1">Link
                        Contact</label>
                    <select x-model="newAccount.contactId"
                        class="w-full px-6 py-4 bg-gray-50 border border-gray-100 rounded-2xl font-semibold outline-none focus:border-gray-900 transition-all appearance-none"
                        required>
                        <option value="">Select Contact</option>
                        <template x-for="c in contacts" :key="c._id">
                            <option :value="c._id" x-text="c.name"></option>
                        </template>
                    </select>
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div class="space-y-2">
                        <label class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1">Type</label>
                        <select x-model="newAccount.accountType"
                            class="w-full px-4 py-4 bg-gray-50 border border-gray-100 rounded-2xl font-semibold outline-none appearance-none">
                            <option value="Payable">Payable</option>
                            <option value="Receivable">Receivable</option>
                            <option value="Operational">Operational</option>
                        </select>
                    </div>
                    <div class="space-y-2">
                        <label class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1">Alias</label>
                        <input type="text" x-model="newAccount.accountName" placeholder="Ledger Name"
                            class="w-full px-4 py-4 bg-gray-50 border border-gray-100 rounded-2xl font-semibold outline-none">
                    </div>
                </div>
                <button type="submit"
                    class="w-full py-5 bg-gray-900 text-white rounded-2x font-bold uppercase tracking-widest text-[10px] hover:bg-gray-800 transition-all shadow-xl shadow-gray-200">Initialize
                    Account</button>
            </form>
        </div>
    </div>

    <!-- Wide Immersive Voucher Entry Modal -->
    <div x-show="modals.addTransaction" class="fixed inset-0 z-[60] flex items-center justify-center p-6" x-cloak>
        <div @click="modals.addTransaction = false"
            class="absolute inset-0 bg-black/40 backdrop-blur-md animate__animated animate__fadeIn"></div>
        <div
            class="relative w-full max-w-5xl bg-white rounded-[40px] shadow-2xl flex overflow-hidden animate__animated animate__zoomIn">

            <!-- Left: Voucher Entry Form -->
            <div
                class="w-full md:w-[60%] p-10 lg:p-14 border-r border-gray-50 overflow-y-auto max-h-[85vh] custom-scrollbar">
                <div class="flex items-center justify-between mb-8">
                    <div>
                        <h2 class="text-2xl font-black text-gray-900 tracking-tighter"
                            x-text="editMode.transaction ? 'Revise Voucher' : 'Voucher Entry'"></h2>
                        <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mt-1">Operational
                            Cashflow Record</p>
                    </div>
                    <button @click="modals.addTransaction = false; editMode.transaction = false"
                        class="text-gray-300 hover:text-gray-900 transition-colors">
                        <i class="fas fa-times"></i>
                    </button>
                </div>

                <form @submit.prevent="saveTransaction()" class="space-y-6">
                    <!-- Account Selection -->
                    <div class="space-y-2">
                        <label class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1">Party / Ledger
                            Master</label>
                        <select x-model="newTx.accountId"
                            class="w-full px-6 py-4 bg-gray-50 border border-gray-100 rounded-2xl font-bold text-sm outline-none appearance-none focus:bg-white focus:border-gray-900 transition-all"
                            required>
                            <option value="">Choose Ledger Account...</option>
                            <template x-for="acc in accounts" :key="acc._id">
                                <option :value="acc._id" x-text="acc.accountName + ' (' + acc.contact?.name + ')'">
                                </option>
                            </template>
                        </select>
                    </div>

                    <div class="grid grid-cols-2 gap-6">
                        <div class="space-y-2">
                            <label class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1">Voucher
                                Type</label>
                            <div class="flex items-center bg-gray-50 p-1.5 rounded-2xl border border-gray-100">
                                <button type="button" @click="newTx.transactionType = 'Debit'"
                                    :class="newTx.transactionType === 'Debit' ? 'bg-white text-emerald-600 shadow-md translate-y-[-1px]' : 'text-gray-400 hover:text-gray-600'"
                                    class="flex-1 py-3 text-[10px] font-bold uppercase rounded-xl transition-all">Payment
                                    (-)</button>
                                <button type="button" @click="newTx.transactionType = 'Credit'"
                                    :class="newTx.transactionType === 'Credit' ? 'bg-white text-rose-600 shadow-md translate-y-[-1px]' : 'text-gray-400 hover:text-gray-600'"
                                    class="flex-1 py-3 text-[10px] font-bold uppercase rounded-xl transition-all">Receipt
                                    (+)</button>
                            </div>
                        </div>
                        <div class="space-y-2">
                            <label class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1">Voucher
                                Amount (₹)</label>
                            <input type="number" x-model="newTx.amount" placeholder="0.00"
                                class="w-full px-4 py-3.5 bg-gray-50 border border-gray-100 rounded-2xl font-black text-xl outline-none focus:bg-white focus:border-gray-900 placeholder:text-gray-300"
                                required>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 gap-6">
                        <div class="space-y-2">
                            <label
                                class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1 flex items-center justify-between">
                                <span>Narration Tag</span>
                                <button type="button" @click="modals.manageTags = true"
                                    class="text-indigo-500 hover:underline">Quick Add +</button>
                            </label>
                            <select x-model="newTx.tagId"
                                class="w-full px-4 py-4 bg-gray-50 border border-gray-100 rounded-2xl font-bold text-xs outline-none appearance-none focus:bg-white focus:border-gray-900 transition-all">
                                <option value="">General Entry</option>
                                <template x-for="tag in tags" :key="tag._id">
                                    <option :value="tag._id" x-text="tag.name"></option>
                                </template>
                            </select>
                        </div>
                        <div class="space-y-2">
                            <label class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1">Effective
                                Date</label>
                            <input type="date" x-model="newTx.date"
                                class="w-full px-4 py-4 bg-gray-50 border border-gray-100 rounded-2xl text-[11px] font-bold outline-none focus:bg-white focus:border-gray-900">
                        </div>
                    </div>

                    <div class="space-y-2">
                        <label class="text-[10px] font-bold text-gray-400 uppercase tracking-widest ml-1">Master
                            Narration / Brief Description</label>
                        <textarea x-model="newTx.description" placeholder="Being amount settled/taken for..."
                            class="w-full px-6 py-4 bg-gray-50 border border-gray-100 rounded-2xl text-sm font-medium h-24 resize-none outline-none focus:bg-white focus:border-gray-900 transition-all"></textarea>
                    </div>

                    <div class="pt-4">
                        <button type="submit"
                            class="w-full py-5 bg-gray-900 text-white rounded-[24px] font-black uppercase tracking-[0.2em] text-[11px] hover:bg-black hover:shadow-2xl transition-all shadow-xl shadow-gray-200 active:scale-[0.98]"
                            :disabled="loading">
                            <span
                                x-text="loading ? 'Processing...' : (editMode.transaction ? 'Update Voucher' : 'Post Voucher to Ledger')"></span>
                        </button>
                    </div>
                </form>
            </div>

            <!-- Right: Account Context Preview (Tally Feel) -->
            <div
                class="hidden md:flex flex-col w-[40%] bg-gray-50/70 p-12 overflow-y-auto max-h-[85vh] custom-scrollbar">
                <template x-if="newTx.accountId">
                    <div class="animate__animated animate__fadeIn space-y-8">
                        <div>
                            <span
                                class="text-[9px] font-bold text-indigo-500 uppercase tracking-[0.2em] block mb-2">Ledger
                                Position</span>
                            <h3 class="text-xl font-bold text-gray-900" x-text="getContextAccount()?.accountName"></h3>
                            <p class="text-xs text-gray-400 mt-1"
                                x-text="getContextAccount()?.contact?.companyName || 'Private Party'"></p>
                        </div>

                        <!-- Mini Balance Card -->
                        <div
                            class="bg-white p-6 rounded-[28px] border border-gray-100 shadow-sm relative overflow-hidden">
                            <div class="absolute top-0 right-0 p-3 text-gray-100">
                                <i class="fas fa-balance-scale-left text-3xl"></i>
                            </div>
                            <span
                                class="text-[9px] font-bold text-gray-400 uppercase tracking-widest block mb-1">Closing
                                Balance</span>
                            <p class="text-2xl font-black text-gray-900">₹<span
                                    x-text="formatNumber(getContextAccount()?.totalBalance)">0</span></p>
                        </div>

                        <!-- Recent Transaction Footprint -->
                        <div>
                            <span class="text-[9px] font-bold text-gray-400 uppercase tracking-widest block mb-4">Past
                                Footprint</span>
                            <div class="space-y-3">
                                <p class="text-[10px] text-gray-400 italic">History context loaded in gateway.</p>
                            </div>
                        </div>

                        <!-- Smart Tip -->
                        <div class="p-5 bg-indigo-50/50 rounded-2xl border border-indigo-100">
                            <p class="text-[10px] text-indigo-600 font-bold flex items-center gap-2">
                                <i class="fas fa-lightbulb"></i>
                                Professional Tip
                            </p>
                            <p class="text-[10px] text-indigo-500/80 mt-2 leading-relaxed">Ensure Narration contains
                                invoice numbers for faster audits later.</p>
                        </div>
                    </div>
                </template>
                <template x-if="!newTx.accountId">
                    <div class="h-full flex flex-col items-center justify-center text-center space-y-6 opacity-40">
                        <div class="w-16 h-16 bg-gray-200 rounded-[28px] flex items-center justify-center text-white">
                            <i class="fas fa-receipt text-2xl"></i>
                        </div>
                        <p class="text-[10px] font-bold uppercase tracking-widest text-gray-400">Context will appear
                            here<br>after account selection</p>
                    </div>
                </template>
            </div>
        </div>
    </div>

    <!-- Manage Tags Modal -->
    <div x-show="modals.manageTags" class="fixed inset-0 z-[60] flex items-center justify-center p-6" x-cloak>
        <div @click="modals.manageTags = false"
            class="absolute inset-0 bg-black/40 backdrop-blur-md animate__animated animate__fadeIn"></div>
        <div class="relative w-full max-w-md bg-white rounded-[40px] shadow-2xl p-10 animate__animated animate__zoomIn">
            <div class="flex items-center justify-between mb-8">
                <div>
                    <h2 class="text-2xl font-black text-gray-900 tracking-tighter">Manage Tags</h2>
                    <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mt-1">Classify your entries
                    </p>
                </div>
                <button @click="modals.manageTags = false"
                    class="w-10 h-10 rounded-full bg-gray-50 flex items-center justify-center text-gray-400 hover:text-gray-900 transition-all">
                    <i class="fas fa-times"></i>
                </button>
            </div>

            <!-- Add New Tag -->
            <form @submit.prevent="createTag()" class="mb-10 space-y-4">
                <div class="grid grid-cols-2 gap-4">
                    <input type="text" x-model="newTag.name" placeholder="Tag Name (e.g. Rent, Tea)"
                        class="w-full px-4 py-4 bg-gray-50 border border-gray-100 rounded-2xl text-[10px] font-bold outline-none focus:bg-white focus:border-gray-900"
                        required>
                    <input type="color" x-model="newTag.color"
                        class="w-full h-[54px] p-1 bg-gray-50 border border-gray-100 rounded-2xl cursor-pointer">
                </div>
                <button type="submit"
                    class="w-full py-4 bg-gray-900 text-white rounded-2xl font-bold uppercase tracking-widest text-[9px] hover:bg-black transition-all shadow-lg active:scale-95">Create
                    New Tag</button>
            </form>

            <!-- Tag List -->
            <div class="space-y-3 max-h-[300px] overflow-y-auto custom-scrollbar pr-2">
                <template x-for="tag in tags" :key="tag._id">
                    <div
                        class="flex items-center justify-between p-4 bg-gray-50/50 rounded-2xl border border-gray-100 group">
                        <div class="flex items-center gap-3">
                            <div class="w-3 h-3 rounded-full" :style="`background-color: ${tag.color}`"></div>
                            <span class="text-xs font-bold text-gray-700" x-text="tag.name"></span>
                        </div>
                        <button @click="deleteTag(tag._id)" class="text-gray-300 hover:text-rose-500 transition-all">
                            <i class="fas fa-trash-alt text-[10px]"></i>
                        </button>
                    </div>
                </template>
            </div>
        </div>
    </div>
</div>