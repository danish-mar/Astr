<!DOCTYPE html>
<html lang="en" class="scroll-smooth">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= product.name %> | <%= shopSettings.shopName %></title>
    <meta name="description" content="<%= product.description ? product.description.substring(0, 160) : 'View ' + product.name + ' at ' + shopSettings.shopName %>">

    <!-- Open Graph / Facebook / Instagram -->
    <meta property="og:type" content="product">
    <meta property="og:url" content="<%= typeof productUrl !== 'undefined' ? productUrl : '' %>">
    <meta property="og:title" content="<%= product.name %> | <%= shopSettings.shopName %>">
    <meta property="og:description" content="<%= product.description ? product.description.substring(0, 160) : 'Official Product Catalog Item' %>">
    <meta property="og:image" content="<%= imageUrls && imageUrls.length > 0 ? imageUrls[0] : '' %>">
    <meta property="og:site_name" content="<%= shopSettings.shopName %>">
    <meta property="product:price:amount" content="<%= product.price %>">
    <meta property="product:price:currency" content="INR">
    <meta property="product:availability" content="<%= product.isSold ? 'out of stock' : 'instock' %>">

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:url" content="<%= typeof productUrl !== 'undefined' ? productUrl : '' %>">
    <meta name="twitter:title" content="<%= product.name %> | <%= shopSettings.shopName %>">
    <meta name="twitter:description" content="<%= product.description ? product.description.substring(0, 160) : 'Official Product Catalog Item' %>">
    <meta name="twitter:image" content="<%= imageUrls && imageUrls.length > 0 ? imageUrls[0] : '' %>">

    <!-- Structured Data (JSON-LD) for Search Engines & Platforms -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org/",
      "@type": "Product",
      "name": "<%= product.name %>",
      "image": <%= JSON.stringify(imageUrls || []) %>,
      "description": "<%= product.description || '' %>",
      "brand": {
        "@type": "Brand",
        "name": "<%= shopSettings.shopName %>"
      },
      "offers": {
        "@type": "Offer",
        "url": "<%= typeof productUrl !== 'undefined' ? productUrl : '' %>",
        "priceCurrency": "INR",
        "price": "<%= product.price %>",
        "availability": "<%= product.isSold ? 'https://schema.org/OutOfStock' : 'https://schema.org/InStock' %>",
        "seller": {
          "@type": "Organization",
          "name": "<%= shopSettings.shopName %>"
        }
      }
    }
    </script>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <style>
        [x-cloak] { display: none !important; }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #ffffff;
            color: #0f172a;
        }

        .mono { font-family: 'JetBrains Mono', monospace; }

        .industrial-grid {
            background-size: 40px 40px;
            background-image: 
                linear-gradient(to right, #f1f5f9 1px, transparent 1px),
                linear-gradient(to bottom, #f1f5f9 1px, transparent 1px);
        }

        .technical-line {
            height: 1px;
            background: linear-gradient(90deg, #000 0%, transparent 100%);
        }

        .image-stage {
            background: #fff;
            border: 2px solid #000;
            cursor: zoom-in;
        }

        .reveal {
            opacity: 0;
            transform: translateY(10px);
            transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        }

        .reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* Smooth lightbox overlay */
        .lightbox-overlay {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
        }
    </style>
</head>

<body class="industrial-grid min-h-screen" x-data="{ 
    activeImg: 0, 
    zoom: false,
    images: <%= JSON.stringify(imageUrls || []) %>,
    init() {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) entry.target.classList.add('visible');
            });
        }, { threshold: 0.1 });
        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
    }
}">

    <!-- Lightbox Modal -->
    <div x-show="zoom" x-cloak 
         class="fixed inset-0 z-[100] flex items-center justify-center lightbox-overlay p-4 md:p-12"
         @keydown.escape.window="zoom = false"
         x-transition:enter="transition ease-out duration-300"
         x-transition:enter-start="opacity-0"
         x-transition:enter-end="opacity-100"
         x-transition:leave="transition ease-in duration-200"
         x-transition:leave-start="opacity-100"
         x-transition:leave-end="opacity-0">
        
        <button @click="zoom = false" class="absolute top-8 right-8 w-12 h-12 bg-black text-white flex items-center justify-center hover:scale-110 transition-transform z-[110]">
            <i class="fas fa-times"></i>
        </button>

        <div class="relative w-full h-full flex flex-col items-center justify-center">
            <img :src="images[activeImg]" class="max-w-full max-h-[80vh] object-contain">
            
            <div class="mt-8 flex gap-4" x-show="images.length > 1">
                <button @click="activeImg = (activeImg - 1 + images.length) % images.length" class="w-12 h-12 border-2 border-black flex items-center justify-center hover:bg-black hover:text-white transition-all">
                    <i class="fas fa-chevron-left"></i>
                </button>
                <button @click="activeImg = (activeImg + 1) % images.length" class="w-12 h-12 border-2 border-black flex items-center justify-center hover:bg-black hover:text-white transition-all">
                    <i class="fas fa-chevron-right"></i>
                </button>
            </div>
        </div>
    </div>

    <!-- Technical Header -->
    <header class="border-b-2 border-black sticky top-0 bg-white/90 backdrop-blur-md z-40">
        <div class="max-w-7xl mx-auto px-4 md:px-6 h-16 md:h-20 flex items-center justify-between">
            <div class="flex items-center gap-3">
                <div class="w-8 h-8 md:w-10 md:h-10 bg-black text-white flex items-center justify-center">
                    <i class="fas fa-store text-xs"></i>
                </div>
                <div>
                    <h1 class="text-sm md:text-lg font-bold leading-none tracking-tight uppercase"><%= shopSettings.shopName %></h1>
                    <p class="text-[8px] md:text-[9px] font-bold text-slate-500 tracking-[0.2em] uppercase mt-0.5 md:mt-1">Official Product Catalog</p>
                </div>
            </div>
            <div class="flex items-center gap-2">
                <div class="w-1.5 h-1.5 md:w-2 md:h-2 rounded-full bg-emerald-500 animate-pulse"></div>
                <span class="text-[8px] md:text-[10px] font-bold uppercase tracking-widest text-slate-600">Store Open</span>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 md:px-6 py-8 md:py-12">
        <!-- Breadcrumb / ID -->
        <div class="mb-6 md:mb-8 flex items-center gap-4 reveal">
            <span class="px-2 py-0.5 md:px-3 md:py-1 bg-black text-white text-[8px] md:text-[10px] font-bold uppercase tracking-widest whitespace-nowrap">
                <%= product.category?.name || 'Product' %>
            </span>
            <div class="technical-line flex-1 opacity-20"></div>
            <span class="mono text-[8px] md:text-[10px] font-bold text-slate-400 whitespace-nowrap">
                Product ID: <%= product.productID %>
            </span>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12">
            <!-- Left Side: Visuals -->
            <div class="lg:col-span-7 space-y-6">
                <div class="image-stage relative aspect-[4/3] md:aspect-[16/10] overflow-hidden shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] md:shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]"
                     @click="zoom = true">
                    <template x-for="(img, idx) in images" :key="idx">
                        <div class="absolute inset-0 flex items-center justify-center p-4 md:p-8 transition-all duration-500"
                            x-show="activeImg === idx" 
                            x-transition:enter="opacity-0 translate-x-4"
                            x-transition:enter-end="opacity-100 translate-x-0" 
                            x-transition:leave="opacity-100"
                            x-transition:leave-end="opacity-0 -translate-x-4">
                            <img :src="img" class="w-full h-full object-contain filter drop-shadow-xl">
                        </div>
                    </template>

                    <% if (!imageUrls || imageUrls.length === 0) { %>
                        <div class="h-full flex flex-col items-center justify-center text-slate-300 gap-4">
                            <i class="fas fa-image text-6xl md:text-8xl"></i>
                            <span class="text-[8px] md:text-[10px] font-bold uppercase tracking-[0.5em]">No Image Available</span>
                        </div>
                    <% } %>

                    <!-- Gallery Indicators -->
                    <div class="absolute bottom-4 left-0 right-0 flex justify-center gap-1.5 z-20" x-show="images.length > 1">
                        <template x-for="(img, idx) in images" :key="idx">
                            <button @click.stop="activeImg = idx"
                                class="w-2 h-2 border border-black transition-all"
                                :class="activeImg === idx ? 'bg-black w-6' : 'bg-white hover:bg-slate-100'"></button>
                        </template>
                    </div>

                    <!-- Zoom Info -->
                    <div class="absolute top-4 right-4 bg-white/50 backdrop-blur px-2 py-1 border border-black hidden md:flex items-center gap-2 pointer-events-none">
                        <i class="fas fa-search-plus text-[10px]"></i>
                        <span class="text-[8px] font-bold uppercase tracking-widest">Click to see full size</span>
                    </div>
                </div>

                <!-- Notes -->
                <% if (product.notes) { %>
                    <div class="reveal p-6 md:p-8 bg-slate-50 border-l-4 border-black">
                        <h3 class="text-[10px] font-bold text-slate-400 uppercase tracking-[0.3em] mb-3">About this product</h3>
                        <p class="text-lg md:text-2xl leading-relaxed font-semibold text-slate-800">
                            <%= product.notes %>
                        </p>
                    </div>
                <% } %>
            </div>

            <!-- Right Side: Data -->
            <div class="lg:col-span-5 space-y-10">
                <div class="space-y-6">
                    <div>
                        <h1 class="text-3xl md:text-5xl font-bold tracking-tighter leading-none mb-2 uppercase">
                            <%= product.name %>
                        </h1>
                        <p class="mono text-[10px] md:text-xs text-slate-500 font-bold italic">
                            Product from <%= shopSettings.shopName %>
                        </p>
                    </div>

                    <!-- Acquisition Card -->
                    <div class="border-2 border-black p-6 md:p-8 space-y-8 bg-white shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] md:shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]">
                        <div class="flex justify-between items-end">
                            <div class="space-y-1">
                                <span class="text-[8px] md:text-[10px] font-bold text-slate-400 uppercase tracking-widest">Price</span>
                                <div class="flex items-baseline gap-1">
                                    <span class="mono text-2xl md:text-4xl font-bold text-black">
                                        ₹<%= Number(product.price).toLocaleString('en-IN') %>
                                    </span>
                                </div>
                            </div>
                            <div class="text-right">
                                <span class="text-[8px] font-bold text-emerald-600 bg-emerald-50 px-1.5 py-0.5 border border-emerald-100 uppercase">Available</span>
                            </div>
                        </div>

                        <% if (shopSettings.phone) { %>
                            <a href="https://wa.me/<%= shopSettings.phone.replace(/\D/g, '') %>?text=Hello, I am interested in: <%= product.name %> (ID: <%= product.productID %>)"
                                class="hidden md:flex items-center justify-center gap-3 py-4 md:py-6 bg-black text-white hover:bg-slate-800 transition-all active:translate-y-1 shadow-[4px_4px_0px_0px_rgba(59,130,246,1)]">
                                <i class="fab fa-whatsapp text-lg text-emerald-400"></i>
                                <span class="text-[10px] md:text-xs font-bold uppercase tracking-[0.3em]">Contact us on WhatsApp</span>
                            </a>
                        <% } %>
                    </div>

                    <!-- Specs -->
                    <div class="space-y-4">
                        <h3 class="text-[10px] font-bold text-slate-400 uppercase tracking-[0.3em]">Specifications</h3>
                        <div class="grid grid-cols-1 gap-2 md:gap-3">
                            <% 
                                let specsArray = [];
                                if (product.specifications) {
                                    try {
                                        if (typeof product.specifications.entries === 'function') {
                                            specsArray = Array.from(product.specifications.entries());
                                        } else {
                                            specsArray = Object.entries(product.specifications);
                                        }
                                    } catch (e) { console.error(e); }
                                }
                            %>
                            <% specsArray.forEach(([key, value]) => { %>
                                <div class="p-3 md:p-4 border border-slate-100 flex justify-between items-center group hover:border-black transition-colors bg-white">
                                    <span class="text-[8px] md:text-[9px] font-bold text-slate-400 uppercase tracking-widest"><%= key %></span>
                                    <span class="text-[10px] md:text-xs font-bold text-black"><%= value %></span>
                                </div>
                            <% }) %>
                        </div>
                    </div>

                    <!-- Branding Footer Info -->
                    <div class="bg-black text-white p-5 md:p-6 flex items-start gap-4">
                        <div class="w-10 h-10 md:w-12 md:h-12 border border-white/20 flex items-center justify-center shrink-0">
                            <i class="fas fa-store text-sm md:text-lg"></i>
                        </div>
                        <div class="space-y-1 min-w-0">
                            <p class="text-[10px] md:text-xs font-bold uppercase tracking-widest truncate"><%= shopSettings.shopName %></p>
                            <p class="text-[8px] md:text-[10px] text-slate-500 font-medium leading-tight">
                                <%= shopSettings.address %>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Verification -->
        <section class="mt-16 md:mt-24 pt-12 md:pt-16 border-t-2 border-black reveal">
            <div class="flex flex-col md:flex-row gap-10 md:gap-12 items-center justify-between">
                <div class="max-w-xl space-y-4 md:space-y-6 text-center md:text-left">
                    <h2 class="text-3xl md:text-4xl font-bold tracking-tight uppercase leading-none">Verified Product.</h2>
                    <p class="text-slate-500 text-xs md:text-sm leading-relaxed px-4 md:px-0">
                        This product is registered in the <%= shopSettings.shopName %> store. Scan the code to verify its details and warranty.
                    </p>
                    <div class="flex flex-wrap justify-center md:justify-start gap-2 md:gap-4">
                        <div class="px-3 py-1.5 border border-black text-[8px] md:text-[10px] font-bold uppercase tracking-widest">Official Item</div>
                        <div class="px-3 py-1.5 border border-black text-[8px] md:text-[10px] font-bold uppercase tracking-widest">Quality Checked</div>
                    </div>
                </div>
                
                <div class="flex flex-col items-center gap-4">
                    <div class="p-4 md:p-6 bg-white border-2 border-black">
                        <img src="<%= qrDataUrl %>" class="w-40 h-40 md:w-56 md:h-56">
                    </div>
                    <span class="mono text-[8px] font-bold text-slate-400 tracking-[0.4em] text-center"><%= product.productID %></span>
                </div>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer class="mt-16 md:mt-24 bg-slate-50 border-t border-slate-200 py-12 md:py-16">
        <div class="max-w-7xl mx-auto px-4 md:px-6 grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12">
            <div>
                <span class="text-[8px] md:text-[10px] font-bold text-slate-400 uppercase tracking-widest block mb-3">Our Location</span>
                <p class="text-[10px] md:text-xs font-bold text-slate-800 uppercase leading-relaxed">
                    <%= shopSettings.address %>
                </p>
            </div>
            <div>
                <span class="text-[8px] md:text-[10px] font-bold text-slate-400 uppercase tracking-widest block mb-3">Contact us</span>
                <p class="text-[10px] md:text-xs font-bold text-slate-800 mono">Phone: <%= shopSettings.phone %></p>
                <p class="text-[10px] md:text-xs font-bold text-slate-800 mono mt-1">Email: <%= shopSettings.email %></p>
            </div>
            <div class="md:text-right flex flex-col justify-end">
                <p class="text-[8px] font-bold text-slate-400 uppercase tracking-[0.2em]">
                    © <%= new Date().getFullYear() %> <%= shopSettings.shopName %>
                </p>
            </div>
        </div>
    </footer>

    <!-- Mobile Action -->
    <div class="fixed bottom-4 left-4 right-4 md:hidden z-30">
        <a href="https://wa.me/<%= (shopSettings.phone || '').replace(/\D/g, '') %>?text=I am interested in: <%= product.name %> (ID: <%= product.productID %>)"
            class="w-full flex items-center justify-center gap-3 py-4 bg-black text-white shadow-2xl active:translate-y-0.5 transition-transform">
            <i class="fab fa-whatsapp text-lg text-emerald-400"></i>
            <span class="text-[10px] font-bold uppercase tracking-[0.3em]">Send WhatsApp Inquiry</span>
        </a>
    </div>

</body>
</html>